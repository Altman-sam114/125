# v5.8f README / plan / flow 产品定位收口记录

## 目标

- 延续 v5.8 发布候选硬化，把项目交接口径从 “WWIIHexV0 / v0.x 阿登原型” 调整为 “山河一统 Agent / 唐宋 v5.x / `jianlong_960_unification` 当前主线”。
- 让 README、`md/plan/plan.md`、`md/flow/flow.md` 和 `md/flow/flowchart.md` 明确：WWIIHexV0、阿登、Guderian、Bastogne 和 v0.x 分支记录是 legacy 工程名、fallback 或历史地基，不是默认产品叙述。
- 保持源码、JSON、Codable schema、规则、UI 运行行为和 CI workflow 不变。

## 非目标

- 不改 Swift、JSON、Xcode project、workflow 或资源文件。
- 不改变主游戏 `DataLoader` 的 legacy 阿登 fallback。
- 不删除历史 v0.x 记录；只把旧分支段落标成历史兼容附录，避免误导下一轮 Agent。
- 不做本机测试、build、Swift parse、Markdown 检查或 `git diff --check`。

## 关键文件

- `README.md`
- `md/plan/plan.md`
- `md/flow/flow.md`
- `md/flow/flowchart.md`
- `update_log.md`
- `md/prompt/v5.0-唐宋迁移/v5.8f_docs_product_positioning_record.md`

## 实现说明

- README 首屏和当前进度已以唐宋 v5.x 为主线；本轮继续补 v5.8e/v5.8f 阶段索引，并把“给后续 Claude Code 的提示”改为先读 `AGENTS.md`、v5.0 唐宋总提示词、最新 v5.8 阶段记录、flow 和云端 artifact 口径。
- `md/plan/plan.md` 的当前迁移快照更新到 v5.8e/v5.8f；v5.8 路线表补 MapEditor 默认路径硬化和文档定位收口；v5.3 队列改为历史收口记录。
- `md/flow/flow.md` 标题和资料依据改为唐宋 v5.x / main 当前主线；v1.0 / v0.4 分支段落改为历史兼容附录，删除“当前工作树混有旧分支改动”的当前化口吻。
- `md/flow/flowchart.md` 的 MapEditor 默认数据节点改为“缺失时报错，不静默回退阿登”，避免和 v5.8e 行为冲突。
- `update_log.md` 顶部补当前 main-only / cloud validation 制度说明，并新增 v5.8f 记录。

## 并发子 Agent 结果

- README / plan 子 Agent 只读指出：README 标题和当前进度已开始唐宋化，但接手说明、文档索引、plan 进度快照和 v5.8 路线仍落后于 v5.8e。
- flow / mermaid 子 Agent 只读指出：`md/flow/flow.md` 标题和资料依据仍像 v0.5 当前分支，历史 v1.0 / v0.4 段落仍有当前化措辞；`md/flow/flowchart.md` 的 MapEditor 默认数据节点仍容易误读为编辑器 fallback 阿登。
- 主线程没有让子 Agent 编辑文件，避免多个 Agent 同时改同一文档。

## 验证口径

- 按用户当前要求，本轮不在本机运行测试、build、Swift parse、Markdown 检查或 `git diff --check`。
- 本轮完成后直接 commit 并 push 到 `origin/main`，触发 GitHub Actions `WWIIHexV0 CI Results`。
- Agent C / 主线程验收必须下载最新 `origin/main` commit 对应的未加密 artifact，核对 `ci-artifact-manifest.json` 的 `branch`、`commitSha`、`runId`、`runAttempt`，并阅读 `junit.xml`、`static-checks.log`、`xcodebuild.log` 和 `ci-failure-summary.md`。

## 遗留风险

- 主游戏 `DataLoader` legacy fallback 是否应在发布态显式报错仍未处理。
- 全局 accessibility、截图/布局/VoiceOver、资源授权和完整 v5.8 RC 玩家可见残留清单仍待后续。
- 文档中仍保留大量 v0.x 历史事实；后续应继续压缩“默认主线”入口，但不要删除会影响回归和架构理解的历史记录。
