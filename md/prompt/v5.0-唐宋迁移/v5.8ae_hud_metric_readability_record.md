# v5.8ae HUD 指标与队列读法硬化记录

## 目标

继续收口唐宋默认主路径首屏 HUD 的资源与队列读法，减少裸数字，并让指标行在读屏时保留标签和值的完整上下文。

## 实现范围

- `HUDView`：
  - 唐宋路径的丁口、钱帛、粮草库存值从裸数字改为“丁口 N / 钱帛 N / 粮草 N”。
  - 唐宋路径的军备队列数量从裸数字改为“暂无军备”或“军备队列 N 项”。
  - HUD 指标行补充合并后的 accessibility label/value。
- 文档：
  - 同步 `README.md`、`md/plan/plan.md`、`md/flow/flow.md`、`md/flow/flowchart.md` 和 `update_log.md`。
  - 将 v5.8ad 的云端通过结果补写到 `update_log.md`，保留 v5.9 未开始边界。

## 并发子 Agent

- 主游戏 UI 只读扫描指出 `HUDView` 的丁口、钱帛、粮草和军备队列仍以裸数字展示，且 `metric` 缺少合并读屏语义。
- 文档只读扫描指出下一轮应命名为 v5.8ae 小切片，并继续保持 v5.9 发布验收未开始，不得把单个小切片云端通过写成发布级验收完成。

## 禁止项

- 不改变 `EconomyState`、`FactionEconomyLedger`、生产队列、资源规则、命令、胜利规则、日志结构或 Codable schema。
- 不改变 HUD 的布局结构、目标锚点、下一步提示、结束回合按钮或亲征/观战权限。
- 不声明完整 VoiceOver 实机验收、截图布局验收、全项目玩家可见残留清零或发布级 UI 完成。
- 按用户要求，本机不运行测试、build、Swift parse、Markdown 检查、`git diff --check`、模拟器或截图。

## 验收口径

- 本地只允许读文件、审查 diff/status、commit/push。
- 推送 `origin/main` 后由 GitHub Actions 执行云端 build/static checks，并由 artifact manifest、JUnit、主构建日志和失败摘要核对结果。
- 未跑本机重测试、模拟器、截图和 VoiceOver 实机验收；不得把本轮写成完整全项目写入端本地化或发布级 UI 验收完成。

## 遗留风险

- 将领军令面板指标、战报行整行读屏、低频面板 raw id / 英文 fallback 和其它短数字读法仍需继续按 v5.8/v5.9 小切片扫描。
- 完整 VoiceOver 实机、截图布局、全项目玩家可见残留扫描、真 LLM 输出本地化和发布级 UI 验收仍未完成。
